# Chapter Tracker

Status legend: **Draft** | **Review** | **Final** | Planned

## Part 1: First Contact

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 1.1 Your First Rust Program | [`src/part1/1.1 Your First Rust Program.md`](../src/part1/1.1%20Your%20First%20Rust%20Program.md) | **Review** | Philosophy-first intro (beginner-accessible opening, no jargon), install, Cargo, printing/formatting, **format specifiers** (precision `{:.2}`, width/alignment `{:>10}`/`{:<10}`/`{:^10}`, zero-padding `{:05}`, hex `{:#x}`, binary `{:#b}`, combined specifiers, `format!` macro for building strings); first-mention styling for rustup/Cargo/crate; **forward reference fix** (replaced "You will learn these rules naturally over the next few chapters" with self-contained ownership/borrowing summary); all 7 examples verified Rust 1.93.1 |
| 1.2 The Compiler Is Your Ally | [`src/part1/1.2 The Compiler Is Your Ally.md`](../src/part1/1.2%20The%20Compiler%20Is%20Your%20Ally.md) | **Review** | Error anatomy (E0382 with clone suggestion, E0308 with function-defined-here, unused variable warning), `cargo check`, `cargo clippy` (needless_range_loop with array, bool_comparison), **`dbg!` macro** (runtime value tracing, expression+value+location, stderr separation, pass-through return, `println!` vs `dbg!` guidance), **`eprintln!` macro** (production error/warning output to stderr, stdout vs stderr separation in piped contexts, `println!`/`eprintln!`/`dbg!` usage guidance), `cargo fmt` with before/after, quality baseline workflow (updated to include `dbg!` step); **forward reference fix** (removed "later chapter on ownership" deferral — self-contained explanation preserved); all outputs verified Rust 1.93.1 |

## Part 2: Thinking in Values

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 2.1 Variables, Expressions, and Control Flow | [`src/part2/2.1 Variables, Expressions, and Control Flow.md`](../src/part2/2.1%20Variables,%20Expressions,%20and%20Control%20Flow.md) | **Review** | Immutability, shadowing, scalar types, type inference, **integer overflow** (debug panic/release wrap, checked/saturating/wrapping/strict methods), **`strict_*` dedicated section** (Rust 1.91: "Overflow that is always a bug" — debug-vs-release gap philosophy, `strict_add` financial example, drop-in `+` replacement with unconditional panic), **`is_multiple_of`** (Rust 1.87: intent-revealing divisibility check, leap year example, zero-safe), **`midpoint`** (Rust 1.85/1.87: overflow-safe averaging, binary search bug motivation, rounds toward zero, all integer+float types), **`cast_signed`/`cast_unsigned`** (Rust 1.87: explicit same-width sign conversion, prevents accidental truncation vs `as`, sensor byte example), **float methods** (`abs`/`sqrt`/`round`/`ceil`/`floor`/`powi`/`clamp`, type suffix `_f64` for method calls), **`round_ties_even`** (IEEE 754 banker's rounding, bias elimination, round vs round_ties_even comparison), **special values and NaN** (0/0=NaN, 1/0=inf, `is_nan`/`is_infinite`/`is_finite`, NaN!=NaN IEEE 754, `PartialEq` not `Eq`, `PartialOrd` not `Ord`, **`total_cmp` inline** — total ordering for floats, production-grade sorting example), **mathematical constants** (`std::f64::consts` module: `PI`/`TAU`/`E`/`SQRT_2`, circle circumference+area example, full module contents referenced; removed `GOLDEN_RATIO`/`EULER_GAMMA` — not yet stable on Rust 1.93.1), constants, statics (`static` vs `const`, when to use each, **`AtomicU32` mutable global example**), **`LazyLock` lazy initialization** (runtime-computed statics, computed-once semantics, replaces `lazy_static`/`once_cell`), expressions, if/loop/while/for, ranges, loop labels, tuples/arrays, **`Vec<T>` mini-intro with `vec!`**, **`usize` array indexing demo**, **numeric type conversions with `as`** (widening vs narrowing, `sum as f64 / len() as f64` averaging pattern, closes gap where `as f64` was used in 10+ later examples without introduction), if let, let chains (2024, **self-contained `Result` box mental model + `if let` reading tip**); **chapter structure pass** (How to Read box, overflow decision mermaid diagram, Equation 2-1, titled Examples 2-1 through 2-4, inline Tip/Warning/Check Yourself blocks, Why This Matters + end-of-chapter retrieval prompts); all forward references replaced with inline explanations; **anti-stacking fix** (Control Flow A-head intro text), **forward reference fixes** (Vec "later chapters" → natural phrasing, `if let` Part 3 reference → self-contained description); **turbofish syntax** (`::<>` type-on-call notation, "21".parse::<i32>() example, named and explained as alternative to let-binding annotation, community etymology, bridges all subsequent `::<>` uses across book); **forward reference fix** (narrowing conversions: replaced "later in the book, you will see safer alternatives" with inline `u8::try_from()` code example — shows `Err(TryFromIntError(()))` for overflow, `Ok(200)` for success, bridges to `TryFrom` trait in 4.1 and `as` anti-pattern in 7.2); **`const fn` bridge** (constants rule 3 now mentions `const fn` by name, bridging to formal introduction in 2.2); **forward reference fix** (`total_cmp`: replaced "when you reach iterators" deferral with inline code example and self-contained explanation; updated 4.3 to reference 2.1 introduction); let-chains opener reverified Rust 1.94.0; **added Exercises section** (3 micro-projects with solutions: Exercise 2-1 Temperature Report — for loop, `if` as expression, format specifiers, `f64` arithmetic; Exercise 2-2 Saturating Scoreboard — `u8` saturating arithmetic, `mut`, `if` expression for status; Exercise 2-3 Input Validator — let chains, `parse` with turbofish, `Vec::new`/`push`, combined parse+range validation); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; **renumbered 2.3 exercises** (Exercise 2-1→2-4, 2-2→2-5, 2-3→2-6; later renumbered to 2-7/2-8/2-9 when 2.2 exercises were added) |
| 2.2 Functions and Closures | [`src/part2/2.2 Functions and Closures.md`](../src/part2/2.2%20Functions%20and%20Closures.md) | **Review** | Functions (params, return, implicit return, early return), **placeholder macros (`todo!()`/`unimplemented!()`/`unreachable!()` — top-down development, intent communication, shipping_cost partial implementation example)**, expression-oriented bodies, **`const fn` (compile-time function evaluation, `square`/`factorial` examples, `const` declaration usage, runtime dual-use, bridges back to 2.1 `const` and `const fn` standard library methods)**, closures (syntax, capture modes, Fn/FnMut/FnOnce traits), higher-order functions, returning closures, function factories; **fixed `apply_to_each`** (replaced hardcoded `[i32; 3]` return with proper slice iteration, consistent with `convert_temps` pattern); **fixed `move` forward reference** (replaced "you will see why in the next chapter" with self-contained explanation of ownership transfer, dangling borrow prevention, and natural bridge to `make_greeter`); **`&'static str` intuition pass** (same `&str`, stronger lifetime guarantee, string literals as always-available text, closure owns the reference not a heap `String`); **forward reference fixes** (Option: removed "in depth in Part 3" deferral; `impl Trait`: replaced "full generic syntax in Part 4" with inline monomorphization explanation); **`const fn` gap fix** (term was used in 2.1 `midpoint`/`cast_signed`/`cast_unsigned` without explanation; now formally introduced before closures); **added Exercises section** (3 micro-projects with solutions: Exercise 2-4 Tip Calculator — functions, `const fn`, early return with `Option`, `match`, formatted output; Exercise 2-5 Word Counter — higher-order function with `impl Fn`, closures capturing environment variables; Exercise 2-6 Formatter Factory — returning closures with `impl Fn`, `move` keyword, function factories); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; **renumbered 2.3 exercises** (Exercise 2-4→2-7, 2-5→2-8, 2-6→2-9 for sequential numbering across Part 2); all 17 chapter examples + 3 exercise solutions verified Rust 1.93.1/1.94.0 |
| 2.3 Ownership | [`src/part2/2.3 Ownership.md`](../src/part2/2.3%20Ownership.md) | **Review** | Three rules of ownership, stack vs heap, move semantics, **moves into closures (`move` keyword callback from 2.2, closure outliving scope)**, Copy types, Clone, Drop/RAII, **drop on reassignment** (mutable variable reassignment drops previous value immediately, Noisy struct timing demo, ownership rule two connection), drop order, early drop, std::mem::drop; **added String::from vs string literal explanation** (D-head, `&str` vs `String` distinction, why String demonstrates ownership), **visual pass** (replaced two ASCII ownership diagrams with titled Mermaid figures for stack/heap layout and would-be double-free layout), **chapter structure pass** (How to Read box, Example 2-5 through 2-8 titles, Sidebar clarifying `Noisy` as instrumentation, end-of-chapter Check Yourself prompts), **updated E0382 error output** to Rust 1.93.1 (column 5:22→5:21, added `help: consider cloning` suggestion with inline note), **refined data races bullet** (precise ownership+borrowing attribution); **forward reference fixes** (removed "next chapter on borrowing" String/&str deferral; replaced "you will see how in the Clone section later" with self-contained compiler-suggestion description; replaced "borrowing rules you will learn next" in data races bullet with inline borrowing rule summary); **added Exercises section** (3 micro-projects with solutions: Exercise 2-7 Ownership Relay — function chaining with move semantics; Exercise 2-8 Playlist Manager — take-and-return ownership pattern with Vec, shadowing, explicit drop; Exercise 2-9 Cleanup Log — custom Drop impl, scope-based cleanup, reverse drop order prediction; updated closing paragraph to reference Exercise 2-8 friction as motivation for borrowing); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; all 15 chapter examples + 3 exercise solutions verified |
| 2.4 Borrowing and References | [`src/part2/2.4 Borrowing and References.md`](../src/part2/2.4%20Borrowing%20and%20References.md) | **Review** | Shared refs (`&T`), mutable refs (`&mut T`), **dereference operator (`*`)** (automatic dereferencing for methods vs explicit `*` for direct value access, `&`/`*` as complementary operations, `double(&mut i32)` example), many-readers-or-one-writer rule, NLL, borrow checker errors (E0596/E0499/E0502/E0106), slices (`&str`/`&[T]`), dangling references, lifetimes (`'a`), **lifetime violation example (E0597 does_not_compile)**, lifetime elision, practical patterns; **chapter structure + visual pass** (How to Read box, Figure 2-4 Mermaid diagram replacing the ASCII memory sketch, Example 2-9 through 2-12 titles, `&str` Tip, lifetime Sidebar, end-of-chapter Check Yourself prompts), **updated E0596 error output** (added `help: consider changing this to be a mutable reference` suggestion), **updated E0106 error output** (added `help: return an owned value` suggestion), **heading case fix** (`Working with` lowercase preposition); **added Exercises section** (3 micro-projects with solutions: Exercise 2-10 Playlist Stats Borrowing Edition — `&[String]` shared borrows, returning `&str` from slice, contrast with Exercise 2-8 ownership friction; Exercise 2-11 In-Place Temperature Converter — `&mut [f64]` mutable slices, `*` dereference, read-then-write pattern, `clamp`; Exercise 2-12 Word Analyzer — `&str` parameters, `find`+slicing word extraction, returning borrowed slices, shared-to-mutable borrow transition); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; all outputs verified Rust 1.93.1 |

## Part 3: Modeling Your Domain

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 3.1 Structs and Methods | [`src/part3/3.1 Structs and Methods.md`](../src/part3/3.1%20Structs%20and%20Methods.md) | **Review** | Struct definition, field init shorthand, mutation, struct update syntax (move visible in output), tuple structs, unit structs, methods (`&self`/`&mut self`/`self`), associated functions, `new` convention, `#[derive(Debug)]`, `Display` impl, ownership with structs, capstone example; **fixed dead_code/unused warnings** (all 18 examples zero-warning), **updated E0277 error output** to Rust 1.93.1; **forward reference fix** (unit structs: replaced "you will see them used with traits in Part 4" with self-contained sort-order example); **added Exercises section** (3 micro-projects with solutions: Exercise 3-1 Rectangle Toolkit — tuple struct `Meters` newtype, `new`, `&self` methods, `Display`, method parameters with `&Rectangle`; Exercise 3-2 Student Gradebook — `&mut self` mutation with `Vec::push`, `&self` queries, `Debug` derive, `Display` impl, `passing` threshold method; Exercise 3-3 Bank Account Ledger — all three receivers, consuming `close(self)`, free function borrowing `&BankAccount`, transaction recording, failed withdrawal guard); all exercise solutions verified zero-warning Rust 1.94.0; all outputs verified Rust 1.93.1 |
| 3.2 Enums and Pattern Matching | [`src/part3/3.2 Enums and Pattern Matching.md`](../src/part3/3.2%20Enums%20and%20Pattern%20Matching.md) | **Review** | ADTs (data-carrying variants, named fields), **methods on enums** (`impl` blocks with `match self`, `&self`/`self` receivers, TrafficLight state transition example, bridge from struct methods in 3.1), exhaustive `match` (E0004 with `note: defined here` + `help: todo!()` suggestion), match as expression, wildcard `_`, or patterns (`\|`), **`matches!` macro** (dedicated subsection, idiomatic boolean pattern check), `if let`, **"When to Prefer Match over If Let"** (exhaustiveness note), **`while let`** (loop counterpart of `if let`, `Vec::pop()` stack-drain example, closes gap where `while let` was used in 4 later chapters without introduction), **`let`-`else`** (guard clauses, happy path flat, divergent else block, when-to-use-each guide), let chains (Rust 2024), destructuring (enums, structs, tuples, nested), match guards, `..` ignore pattern, **`@` bindings** (bind value while testing pattern, range validation, enum variant data validation, HTTP status code and Command examples), `Box<Expr>` recursive types, capstone Ticket example; **fixed dead_code/unused warnings** (all 24 examples zero-warning), **updated E0004 error output** to Rust 1.93.1; **forward reference fix** (Box: replaced "in detail in Part 5" with self-contained heap-pointer explanation); **added Exercises section** (3 micro-projects with solutions: Exercise 3-4 Temperature Station — enum with data variants, methods with match, Display, match guards, `matches!` macro, `is_freezing`/`category`/`to_celsius` methods; Exercise 3-5 Token Classifier — `let`-`else` early return, `while let` with `Vec::pop()`, `@` range bindings, let chains for operator classification; Exercise 3-6 Access Log Analyzer — struct+enum composition, let chains (Rust 2024), or patterns, `matches!`, destructuring `Role::User(name)` in let chains); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; all outputs verified Rust 1.93.1 |
| 3.3 Null, Errors, and the Type System | [`src/part3/3.3 Null, Errors, and the Type System.md`](../src/part3/3.3%20Null,%20Errors,%20and%20the%20Type%20System.md) | **Review** | `Option<T>` (Some/None, unwrap_or, map, and_then, **`or`/`or_else` (alternative fallback, layered lookup chain, lazy evaluation, and_then complement)**, **`filter` (predicate-based keep/discard, gate analogy, bridges to iterator filter in 4.3)**, **`flatten` (nested Option collapse, HashMap<K, Option<V>> pattern, and_then relationship)**, is_some_and, is_none_or), `Result<T, E>` (Ok/Err, map, map_err, unwrap_or, **`flatten` (Rust 1.89: nested Result collapse, map-with-fallible-function pattern, and_then relationship)**), `?` operator (Result and Option, **automatic error conversion** — `&str`→`String` example demonstrating `From`-based conversion without forward reference), Option↔Result conversion (ok_or, ok), unwrap/expect guidance, making illegal states unrepresentable (enum vs boolean flags, newtype wrappers, validated construction, **module privacy for invariant enforcement**), capstone Score/grade example; **chapter structure pass** (How to Read box, Example 3-1 through 3-5 titles, `?` Sidebar, `unwrap` Warning, end-of-chapter Check Yourself prompts), **stabilized parse-error output** (Display-based `ParseIntError` rendering in the parsing and `?` examples, re-verified in `./build`), **capstone clarity pass** (replaced early `.iter().filter_map(...).collect()` with explicit `Vec::push` collection inside `process_scores`, keeping the chapter self-contained before iterators), **platform-neutral `expect` example** (`split_once('=')` invariant instead of `HOME` env var), **fixed dead_code warnings** (Connection enum uses both variants, Percentage destructures OutOfRange, fragile struct marked `rust,ignore`), **updated E0369 error output** to Rust 1.93.1; **fixed `?` operator forward reference** (replaced "From trait, which you will learn about in Part 4" with self-contained explanation and `validate`/`process` code example showing automatic `&str`→`String` conversion); **anti-stacking fix** (Your First Result lead-in sentence); **forward reference fixes** (generics: removed "in detail in Part 4" deferral, added inline "placeholder that lets Option work with any type"; modules: removed "full module system in Part 5" deferral); **added Exercises section** (3 micro-projects with solutions: Exercise 3-7 Config Parser — `Result`/`?` chaining, `ok_or`, `map_err`, `Option` for missing keys, `unwrap_or` default, `split_once`; Exercise 3-8 Inventory Lookup — `Option` combinator chain with `or_else`/`filter`/`map`, `?` with `Option` for early return on absence, layered data source search; Exercise 3-9 Email Validator — validated construction with private fields, custom error enum with `Display`, `?` for chaining validations, `split_once` with `ok_or`, making illegal states unrepresentable); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; all 25 examples verified Rust 1.93.1 |

## Part 4: Abstraction Without Cost

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 4.1 Traits | [`src/part4/4.1 Traits.md`](../src/part4/4.1%20Traits.md) | **Review** | Trait definition/impl, default methods, `impl Trait` params, derive (`Debug`/`Clone`/`PartialEq`/`Eq`/`Hash`/`Default`/`PartialOrd`/`Ord`/`Copy`), `Display` vs `Debug`, **`Display` → `ToString` blanket implementation** (`.to_string()` for free on any `Display` type, Celsius example, never implement `ToString` directly guidance), **`fmt::from_fn` (Rust 1.93: ad-hoc Display/Debug from closure, lazy evaluation, reusable `join` formatter returning `impl Display`)**, `From`/`Into`, `TryFrom`/`TryInto` (with `Display` error impl), `AsRef`, **`AsMut` (mutable reference conversions, `scramble` XOR cipher example accepting `Vec<u8>` and `[u8; N]`, AsRef/AsMut read-vs-write guidance)**, **`FromStr` (the trait behind `.parse()`, Color enum case-insensitive parsing example, `type Err` associated type, `.parse()` and `FromStr::from_str()` equivalence, standard library implementors list)**, **supertraits (`trait Foo: Bar + Baz`, prerequisite traits, Loggable:Display example, links to Error:Debug+Display/Ord:Eq+PartialOrd/Copy:Clone)**, operator traits (`Add`), orphan rule, capstone temperature conversion example; **chapter structure pass** (How to Read box, Figure 4-1 trait-tool mermaid, Example 4-1 and 4-2 titles, `From` Warning, end-of-chapter Check Yourself prompts), **custom `impl Default`** (manual default values for production configs, ServerConfig example with struct update syntax), **`#[default]` enum attribute** (LogLevel enum, unit-variant-only constraint, bridges to `or_default`/`mem::take`/`unwrap_or_default`); **fixed dead_code/unused warnings** (all 22 examples zero-warning), **C-head case fix** (`Function Parameters` → `function parameters`), **heading hierarchy fix** (`from_fn` C-head promoted to B-head — was level-skipping under A-head without parent B-head), **forward reference fixes** (replaced "You will explore the full generics syntax in the next chapter" with self-contained monomorphization explanation, replaced "You will learn more about associated types in the next chapter" with self-contained associated type explanation, added inline generic syntax explanation for `join` function's `<'a, T: fmt::Display>`); **added Exercises section** (3 micro-projects with solutions: Exercise 4-1 Weight Converter — custom `Describable` trait with `Display` supertrait, `From` conversions, `impl Trait` parameter, `.to_string()` from `Display`; Exercise 4-2 Shape Parser — `FromStr` parsing, `Display`, `Add` operator overloading, `Area` newtype, `total_area` accumulation; Exercise 4-3 Task Tracker — manual `Ord`/`PartialOrd` via rank helper, `Default` for `Priority`, `Display`, `AsRef<str>`, `impl Into<String>` constructor, generic `longest_title` function); all exercise solutions verified zero-warning Rust 1.94.0; all outputs verified Rust 1.93.1 |
| 4.2 Generics | [`src/part4/4.2 Generics.md`](../src/part4/4.2%20Generics.md) | **Review** | Generic functions/structs/enums, trait bounds (`T: Trait`), `impl Trait` (argument + return position, **same-type vs different-type code example**: `show_any`/`show_pair`), `where` clauses, **const generics (`<const N: usize>`, compile-time value parameters, array-size generics, type+const composition with `filled` example, foundation for `as_array`/`as_chunks` in later chapters)**, **`impl Trait` in trait methods (RPITIT)** (Rust 1.75: `-> impl Trait` in trait method signatures, Summarizable example with Article/Tweet returning different iterator types, dyn-compatibility note), associated types vs type params, **associated type bounds (`Item: Display`, `Item: Display + Debug`, constraining vs pinning distinction, Rust 1.79/1.92)**, trait objects (`dyn Trait`), dyn compatibility (**added `impl Trait` return rule**), **trait upcasting (Rust 1.86: `dyn SubTrait` → `dyn SuperTrait` coercion, reference and Box examples, vtable pointer rewrite, one-way narrowing semantics)**, monomorphization/zero-cost abstraction, capstone Measurable example; **chapter structure pass** (How to Read box, Figure 4-2 dispatch decision mermaid, Example 4-3 and 4-4 titles, end-of-chapter Check Yourself prompts), **`where`-clause intuition pass** (explains non-parameter bounds as “types built from `T`,” with explicit tuple-bound takeaway), **Rust 2024 RPIT lifetime capture strengthening** (older `+ '_'` spelling note, Tip for reading borrowed `impl Iterator`, new compile-fail escape example showing borrow remains tied to input), **fixed `clone_self` dead_code warning** (added `#[derive(Clone)]` + concrete-type call), **updated E0369 error output** to Rust 1.93.1 (column 2:11→2:10); **forward reference fixes** (removed “chapters ahead” deferral for const generic applications; replaced “Box<T> covered fully in Part 5” with self-contained heap-pointer explanation and backward reference to 3.2; replaced “Chapter 3.2” numbered reference with descriptive “the pattern matching chapter”); **added Exercises section** (3 micro-projects with solutions: Exercise 4-4 Generic Statistics Calculator — generic `Stats<T>` struct, `PartialOrd + Copy` bounds, `Option` for empty slices, `Display` with `where` clause, `impl Display` parameter; Exercise 4-5 Fixed-Size Buffer — `Buffer<T, const N: usize>` const generics + type params, `Result` for fallible push, `impl Iterator` return, `Display` with compound bounds; Exercise 4-6 Page Renderer — `Renderable` trait with `Display` supertrait, static dispatch with `render_all<T: Renderable>`, dynamic dispatch with `Vec<Box<dyn Renderable>>`, homogeneous vs heterogeneous collections); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; all 22 examples zero-warning, updated lifetime examples reverified Rust 1.94 |
| 4.3 Iterators and Functional Patterns | [`src/part4/4.3 Iterators and Functional Patterns.md`](../src/part4/4.3%20Iterators%20and%20Functional%20Patterns.md) | **Review** | Iterator trait (`next`), three iteration modes (iter/iter_mut/into_iter), lazy evaluation, adaptors (map/filter/**`copied`/`cloned` (reference-to-owned conversion, `Copy` vs `Clone` cost distinction, `&&` solution)**/filter_map/**`map_while` (stop-on-failure complement to filter_map, prefix parsing pattern)**/enumerate/zip/**`std::iter::zip` free function (Rust 1.59: symmetric calling style, `IntoIterator` arguments)**/take/skip/flatten/flat_map/chain/**`std::iter::chain` free function (Rust 1.91: symmetric iterator concatenation, mixed collection types via `IntoIterator`)**/inspect/**`peekable` (lookahead without consuming, `peek`/`next_if`/`next_if_eq`, `&&` double-reference note, parsing/grouping use cases, **`next_if_map` (Rust 1.94: consume-and-transform while putting back first non-match, prefix parser example)**)**), **`&&` double-reference pattern explanation** (why filter/find add `&Self::Item`, contrasted with map/any/all/position), **sliding windows** (`windows()` overlapping sub-slices, day-over-day changes + moving average examples, **`array_windows` (Rust 1.94: fixed-size overlapping windows yielding `&[T; N]`, direct destructuring, const-generic bridge)**), consumers (collect/**`unzip` (split iterator of pairs into two collections, coordinate separation + map-then-unzip patterns, single-pass, `Default + Extend` bounds)**/**`partition` (split by predicate into two collections, evens/odds + pass/fail score examples, single-pass, position-based vs predicate-based contrast with unzip)**/fold/**`reduce` (no-initial-value fold, returns `Option<T>` for empty-iterator safety, fold-vs-reduce guidance)**/sum/product/count/any/all/find/position/min/max), **`total_cmp` for float sorting** (PartialOrd vs Ord, NaN safety, production-grade pattern), collecting into Vec/String/HashMap/HashSet/Result, ranges as iterators, `repeat_n`/`once`/`from_fn`/`successors`, implementing Iterator for custom types, IntoIterator, `is_sorted_by_key` (1.82), capstone GradeBook example; **inspect readability pass** (replaced interleaved stderr trace with readable step-by-step flow, notes when `eprintln!` is the better real-world choice), **`collect`-into-`Result` intuition pass** (Display-based parse-error output, inside-out reading bullets, manual-loop mental model), **retrieval practice pass** (new end-of-chapter Check Yourself prompts), **anti-stacking fixes** (Min/Max lead-in sentence, Building Iterators A-head lead-in sentence); **added Exercises section** (3 micro-projects with solutions: Exercise 4-7 Sales Report Pipeline — `map`+`sum`, `max_by`+`total_cmp`, `filter`+`count`, `partition`, `collect`, `is_sorted_by_key`, `Display`; Exercise 4-8 Run-Length Encoder — custom `Iterator` impl, `Peekable`+`next_if_eq`, generic struct with trait bounds, `map`+`collect`, `count`, `max_by_key`; Exercise 4-9 Sensor Dashboard — `fold` multi-accumulator, `windows`, `filter`, `unzip`, `chain`, `enumerate`+`max_by`+`total_cmp`, `from_fn`, `IntoIterator` impl, `Display`); all exercise solutions verified zero-warning Rust 1.94.0; all 41 outputs verified Rust 1.94.0 |

## Part 5: Building Real Things

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 5.1 Error Handling in Practice | [`src/part5/5.1 Error Handling in Practice.md`](../src/part5/5.1%20Error%20Handling%20in%20Practice.md) | **Review** | `Error` trait (Debug+Display, `source()`), error chains, thiserror 2.x (`#[error]`/`#[from]`/`#[source]`/`#[error(transparent)]`), anyhow 1.x (`Result`, `Context`/`with_context`, `bail!`/`ensure!`/`anyhow!`), `?` and `From::from()` composition, **`inspect_err`/`inspect` (Rust 1.76: observe errors in `?` chains without modifying, logging/debugging pattern, `Option::inspect` counterpart)**, library vs application error strategy, **type aliases** (`type Result<T> = std::result::Result<T, MyError>` crate-level pattern, std library `io::Result`/`fmt::Result` examples, bridges `anyhow::Result<T>` and `io::Result<()>`), **fallible main (`fn main() -> Result<(), E>`, Termination trait, error type comparison table, `ExitCode` for specific exit codes, `run()` + `main()` pattern)**, unwrap spectrum, capstone Inventory example; **chapter structure pass** (How to Read box, Figure 5-1 error-strategy mermaid, Example 5-1 and 5-2 titles, error-chain Sidebar, public-API Warning, end-of-chapter Check Yourself prompts), **fixed AppError variant shapes** (distinct error types for `#[source]` vs `#[from]`), **`Send + Sync` forward reference fix** (replaced "you will understand fully in Part 6" with self-contained Send/Sync explanation), **heading style fix** (lowercase crate names), **anti-stacking fix** (Your First anyhow Function lead-in sentence), **Output label fix** (added missing `Output:` labels to 3 Fallible Main section examples for consistency), **platform-neutral `expect` example** (`"8080".parse()` invariant instead of `HOME` env var), **forward reference fix** (replaced "in later chapters" with self-contained phrasing for `io::Result<()>` type alias explanation); **added Exercises section** (3 micro-projects with solutions: Exercise 5-1 Log File Parser — thiserror typed errors, `#[source]`, `map_err`, `type Result<T>` alias, error chain walking, `Display`; Exercise 5-2 Recipe Scaler — anyhow `context`/`with_context`, `ensure!`/`bail!`, `chain()`, `collect::<Result<Vec<_>>>()`, `fn main() -> Result<()>`; Exercise 5-3 Config Validator — thiserror library pattern, `#[source]`, `require`/`require_int` helpers, range validation, error chain with `source()`); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; all 16 chapter examples zero-warning, all outputs verified Rust 1.93.1 |
| 5.2 Collections, Strings, and Smart Pointers | [`src/part5/5.2 Collections, Strings, and Smart Pointers.md`](../src/part5/5.2%20Collections,%20Strings,%20and%20Smart%20Pointers.md) | **Review** | Vec (creation/access/modify/slices/iteration), HashMap (creation/get/entry API/**`or_default` grouping pattern**/**`and_modify` update-or-insert pattern**), HashSet (membership/set ops), **BTreeMap/BTreeSet (sorted collections: sorted iteration, entry API, range queries with `.range()`, BTreeSet, Hash-vs-BTree decision table)**, String vs &str (ownership/conversion/UTF-8), Box (recursive types/trait objects), Rc (shared ownership/refcount), Arc (thread-safe shared ownership), **Cow (clone on write: borrow-vs-own deferral, `normalize` example, `String::from_utf8_lossy` std library usage, pointer decision table)**, capstone Library/Document tag system; **fixed `&&str` double-reference** in HashMap collect example (now uses `into_iter()` on array); **added `get_disjoint_mut`** (Rust 1.86: safe simultaneous mutable access to multiple Vec elements by index, philosophy-first explanation of borrow checker limitation); **added `extract_if`** (Rust 1.87/1.88: conditional removal + collection for Vec/HashMap/HashSet, philosophy-first explanation of retain limitation, lazy iterator semantics; **extended with `BTreeMap`/`BTreeSet` `extract_if`** Rust 1.91: key-range + predicate combination, temperature readings midday window example, updated Hash-vs-BTree comparison table); **added `floor_char_boundary`/`ceil_char_boundary`** (Rust 1.91: safe string truncation at byte budgets, café ☕ example); **added `pop_if`** (Rust 1.86: conditional pop with closure predicate, philosophy-first peek-then-pop limitation, before/after comparison); **added `as_array::<N>()`** (Rust 1.93: safe slice-to-fixed-size-array conversion, RGB pixel parsing example, Option-based length check); **added `as_chunks::<N>()`** (Rust 1.88: compile-time-checked fixed-size slice splitting, 3D coordinates example with remainder handling, `chunks()` vs `as_chunks()` type-level distinction); **added slice patterns** (match on slice shape: `[]`/`[x]`/`[first, .., last]`/`rest @ ..`, Vec matching via `.as_slice()`, let-else guard clause, `@` subslice binding); **added `or_default()` grouping pattern** (Default-based entry insertion, sales-by-day Vec grouping example, explains `or_default()` used without introduction in 4.3 capstone); **added `and_modify()` update-or-insert pattern** (sensor readings example with different update vs insert logic, when-to-use guidance); **updated capstone** `or_insert_with(Vec::new)` → `or_default()` for consistency; **added `split_once`/`rsplit_once`** (first-delimiter string parsing, `Option<(&str, &str)>` return type, key=value and module path examples, `rsplit_once` for last-occurrence splitting); **extended Common String Operations example** with `to_lowercase()`, `ends_with()`, `is_empty()` (completes method pairs, introduces `is_empty()` used 6+ times in later chapters); **added `strip_prefix`/`strip_suffix`** (philosophy-first `Option<&str>` vs `bool` contrast, API URL routing/flag parsing examples, `if let Some(rest)` pattern); **improved string-combination guidance** (ownership-first framing for choosing `push_str`/`push`, `+`, or `format!`; **Figure 5-2** mermaid flowchart; **Example 5-3** showing left-side move and right-side borrow in `first + &second`; warning against repeated concatenation in loops; retrieval-practice prompt); **anti-stacking fixes** (8 B-head lead-in sentences for Creating Vectors, Accessing Elements, Modifying Vectors, Creating and Populating, Looking Up Values, Sorted Iteration, Creating and Converting Strings, Common String Operations; Hash-vs-BTree decision table lead-in sentence); **B-head "over" lowercase fix** (`Iterating Over Vectors` → `Iterating over Vectors`); **added "Sorting Vectors" B-head** (sort/sort_by/sort_by_key, stable vs unstable distinction with task priority example, float sorting with `total_cmp`, `Reverse` wrapper, `is_sorted`, API summary table); **added VecDeque A-head** (double-ended queue: ring buffer philosophy, creation/`from`/`collect`, queue pattern FIFO with `push_back`/`pop_front`, sliding window moving-average example, `pop_front_if`/`pop_back_if` Rust 1.93 conditional dequeue, ring buffer insight with `as_slices`/`make_contiguous`, Vec-vs-VecDeque decision table); **added "Interior Mutability: Cell and RefCell" A-head** (philosophy-first: compile-time borrow checking too conservative for caching/logging/shared state; `Cell<T>` Counter example with `&self` mutation via `get`/`set`, zero overhead for `Copy` types; `RefCell<T>` Logger example with `borrow()`/`borrow_mut()` runtime checking, `Ref<T>`/`RefMut<T>` guards, panic semantics; `Rc<RefCell<T>>` SharedLog example combining shared ownership + mutation; when-to-use decision table; updated pointer decision table and chapter summary); **added `Cell::update`** (Rust 1.88: read-modify-write in a single closure call, balance deposit/withdrawal example, bridges back to Counter `increment` method); **forward reference fixes** (HashMap intro: replaced "BTreeMap — covered later in this chapter" with self-contained description; Rc limitations: replaced "interior mutability — covered in the next section" with inline RefCell explanation; interior mutability table: replaced "Covered in the concurrency chapter" with self-contained description); **anti-stacking fix** ("When to use each" C-head: added lead-in sentence before decision table); **added `const_item_interior_mutations` warning** (Rust 1.93 warn-by-default lint: `const Cell` gotcha with counter example producing `count: 0`, compiler warning output, `Cell` not `Sync` for `static`, correct patterns: Cell in structs/AtomicU32 for globals); **capstone ownership intuition pass** (new intro for why `Rc<Document>` is needed, **Example 5-4** title, `has_tag(&str)` borrowed lookup demo, quick-check output); previous full-chapter verification: all 57 examples zero-warning, outputs verified Rust 1.93.1; updated string-combination example re-verified Rust 1.94.0 |
| 5.3 Modules and Project Structure | [`src/part5/5.3 Modules and Project Structure.md`](../src/part5/5.3%20Modules%20and%20Project%20Structure.md) | **Review** | mod/use/pub, visibility spectrum (pub/pub(crate)/pub(super)/private), struct field visibility, re-exports (pub use), file layout (named-file convention), library vs binary crates, lib+bin pattern, multiple binaries (src/bin/), Cargo.toml (package metadata, dependencies, **`cargo add`/`cargo remove` (command-line dependency management, `--features`/`-F`, `--no-default-features`, `--dev`/`--build`, version resolution, feature display)**, **`[features]` (optional capabilities, defining features, `#[cfg(feature)]`, default features, `default-features = false`, additive rule)**, [lints], resolver v3), **project-local Cargo configuration** (_.cargo/config.toml_ for aliases, env vars, rustflags, repository-local behavior), **config `include` key (Rust 1.94: shared defaults + optional machine-local overrides, left-to-right merge order, including file wins last)**, **TOML 1.1 syntax in Cargo files** (Rust 1.94: multiline inline tables and trailing commas, with toolchain-floor warning), **`#[expect]` vs `#[allow]` (Rust 1.81: checked lint suppression, unfulfilled expectation warning, production-grade alternative to `#[allow]`)**, **automatic cache garbage collection (Rust 1.88: timestamp tracking from 1.78, daily cleanup of unused .crate archives and extracted sources, `[cache] auto-clean-frequency` config, CI caching caveat)**, workspaces (members, workspace.dependencies, workspace.lints), crate evaluation guide, capstone KeyValueStore example; **fixed dead_code/unused warnings** (added `describe` fn using private `is_even`, used `email` pub(crate) field, used FmtResult/IoResult aliases), **fixed anti-stacking** (Package Metadata/Dependencies headings), **renamed `default_settings` to `new`**; **added `[features]` section** (optional capabilities, `#[cfg(feature = "...")]`, default features, `default-features = false`, additive feature rule, temperature formatting example); **added `cargo add`/`cargo remove` section** (command-line dependency workflow, feature flags, dev/build deps, version resolution); **retrieval practice pass** (new end-of-chapter Check Yourself prompts); Cargo config example and TOML 1.1 parsing verified Rust 1.94.0 |
| 5.4 Testing as a First-Class Citizen | [`src/part5/5.4 Testing as a First-Class Citizen.md`](../src/part5/5.4%20Testing%20as%20a%20First-Class%20Citizen.md) | **Review** | `#[test]`/`#[cfg(test)]`, assert family (assert!/assert_eq!/assert_ne! with custom messages), `#[should_panic(expected)]`, Result-returning tests (`?` in tests, `Box<dyn Error>`), `#[ignore]` with reason strings (shown inline in output), integration tests (_tests/_ directory, shared helpers), doc tests (` ``` `/`no_run`/`compile_fail`/`should_panic`, hidden `#` lines), combined doctests (Rust 2024), **documentation generation** (`cargo doc --open`, `//!` crate-level doc comments, section conventions `# Examples`/`# Panics`/`# Errors`/`# Safety`, `cargo doc` vs `cargo test --doc` comparison), `cargo test` workflows (filtering, --show-output, --test-threads, --ignored, --include-ignored, --doc, --lib, --test, --no-fail-fast), capstone Calculator example (`#[derive(Default)]`); **fixed factorial `should_panic` doctest** (explicit bounds check instead of debug-mode overflow), **updated all test output** to Rust 1.93.1 format; **added "Generating Documentation" A-head** (`cargo doc --open`, `//!` inner doc comments, section heading conventions table, generating vs testing command comparison); all outputs verified Rust 1.93.1 |

## Part 6: Fearless Systems Programming

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 6.1 Concurrency Without Data Races | [`src/part6/6.1 Concurrency Without Data Races.md`](../src/part6/6.1%20Concurrency%20Without%20Data%20Races.md) | **Review** | Data race philosophy, `thread::spawn` (JoinHandle, move, `'static`), `thread::scope` (scoped threads, auto-join, **mutable borrowing with `split_at_mut` for parallel disjoint mutation**), **`thread::available_parallelism` (hardware core count query, `div_ceil` chunk sizing, CPU-bound vs I/O-bound thread count guidance)**, Send/Sync (auto traits, Rc non-Send, Cell non-Sync), `Arc<T>` (atomic refcount), `Mutex<T>` (MutexGuard RAII, poisoning, lock scope), **`RwLock<T>` (many-readers-or-one-writer, read/write guards, **`RwLockWriteGuard::downgrade` (Rust 1.92: atomic write-to-read lock conversion, cache population pattern)**, Mutex vs RwLock guidance)**, **atomic types (`AtomicUsize` lock-free counter, `AtomicBool` cancellation flag pattern, `Ordering::Relaxed`/`SeqCst` memory ordering, atomic methods table, atomics-vs-Mutex decision guide)**, `Arc<Mutex<T>>` pattern, channels (`mpsc::channel`, Sender/Receiver, multiple producers), **`io::pipe` (Rust 1.87: anonymous pipes for byte-level inter-thread I/O, `PipeReader`/`PipeWriter` with `Read`/`Write` traits, EOF on writer drop, process I/O redirection)**, **`File::lock` (Rust 1.89: cross-platform file locking, `try_lock`/`lock_shared`/`try_lock_shared` API, `TryLockError::WouldBlock`, advisory on Unix/enforced on Windows, automatic release on drop, lock file pattern)**, async taste (`async`/`await`, Future trait, tokio, `#[tokio::main]`), **async closures (Rust 1.85: `async \|\| {}`, `AsyncFn`/`AsyncFnMut`/`AsyncFnOnce` traits table, `AsyncFnMut` retry example, old `Fn() -> Fut` limitation for higher-ranked lifetimes)**, capstone worker pool; **chapter structure pass** (How to Read box, Figure 6-1 concurrency-tool mermaid, Example 6-1 and 6-2 titles, end-of-chapter Check Yourself + Further Reading sections), **capstone redesign** (replaced locked shared receiver with per-worker inbox channels, result channel, and `Arc<AtomicUsize>` progress counter), **production guidance** (Tip on `mpsc` single-consumer shape plus crossbeam/tokio channel alternatives), **heading style fix** (`The AsyncFn traits` → `The async closure traits`); **added Exercises section** (3 micro-projects with solutions: Exercise 6-1 Parallel Document Analyzer — `thread::scope`, scoped thread borrowing, `Display`, `max_by_key`, parallel result collection; Exercise 6-2 Concurrent Ballot Box — `Arc<Mutex<T>>`, `thread::spawn` with `move`, `MutexGuard` scoping, `HashMap` entry API, `or_default`, `Display`, sorted results; Exercise 6-3 Score Pipeline — `mpsc::channel` multi-stage pipeline, ownership transfer through channels, let chains (Rust 2024), `split_once`, `thread::spawn`, producer/parser/collector pattern); all exercise solutions verified zero-warning zero-clippy Rust 1.94.0; all outputs verified Rust 1.93.1, updated capstone reverified Rust 1.94 |

## Part 7: Idiomatic Rust

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 7.1 Patterns the Pros Use | [`src/part7/7.1 Patterns the Pros Use.md`](../src/part7/7.1%20Patterns%20the%20Pros%20Use.md) | **Review** | Builder (consuming, fallible build, **`#[must_use]` for catching forgotten `.build()` calls**, **`impl Into<String>` boundary explanation with borrowed-vs-owned text example**), newtype (type safety, orphan rule workaround, **explicit-wrapper Tip** explaining why `.0` visibility is intentional and why `Deref` would blur the type boundary), type-state (PhantomData, compile-time state machines, **reject→revise→approve path**, **Figure 7-2** workflow diagram, read-it-aloud orientation for `Document<Draft>`, stronger `PhantomData` intuition as a zero-byte state label), combinators (map/and_then/filter/unwrap_or pipeline, **`Result` combinator quick reference table** with map_err/and_then/unwrap_or_else/ok/**`flatten`**, `parse_port` chain example), extension traits (blanket impls, FooExt convention), **`mem::take`/`mem::replace` (taking ownership from behind a mutable reference, Inbox drain pattern, Machine enum state transition, `mem::swap` mention, standard library usage: `Option::take`/`Vec::drain`)**, capstone document processing system; **chapter structure pass** (How to Read box, Figure 7-1 pattern-selection mermaid, Example 7-1 and 7-2 titles, end-of-chapter Check Yourself prompts), **fixed dead_code warnings** (removed `#[derive(Debug)]` from DatabaseConfig, accessed Email/Server fields directly, demonstrated reject path in type-state), **updated `#[must_use]` warning output** to Rust 1.93.1 (added `#[warn(unused_must_use)]` note, `help: use let _ =` suggestion); **anti-stacking fix** (Option combinators table lead-in sentence); **Filtering section condensed** (standalone `filter` example removed — `filter` now introduced in 3.3; retains combinator chain example); **B-head capitalization fixed** (14 headings to title case), **A-head "over" lowercase fix**; all 17 compilable examples zero-warning, added `Into<String>` example output verified Rust 1.94.0 |
| 7.2 What Not to Do | [`src/part7/7.2 What Not to Do.md`](../src/part7/7.2%20What%20Not%20to%20Do.md) | **Review** | Cloning to silence borrow checker (narrowing scopes, borrowed types, **"When Cloning Is Correct" snapshot code example** — status-tracker history with move on final push), unwrap in production (Result/? propagation, unwrap spectrum, **platform-neutral `expect` example** using a programmer-supplied port literal), Java-in-Rust (getter/setter boilerplate, overusing dyn Trait, Deref inheritance, composition), fighting the type system (stringly-typed code, boolean blindness, invalid states, **`as` cast silent truncation** — narrowing conversions lose data silently, `try_from`/`try_into` safe alternative, when `as` is acceptable), reaching for unsafe (split_at_mut, 2024 unsafe_op_in_unsafe_fn), overusing Rc/Arc (move/borrow first, decision table), index loops vs iterators (translations table), capstone Score/Grade report (uses `total_cmp` for float sorting); **B-head capitalization fixed** (17 headings to title case), **A-head "over" lowercase fix**, **anti-stacking fixes** (lead-in sentence for Return Result section, When Smart Pointers Are Correct table, Common Translations table); all 28 examples zero-warning, all outputs verified Rust 1.94 |
| 7.3 Where to Go from Here | [`src/part7/7.3 Where to Go from Here.md`](../src/part7/7.3%20Where%20to%20Go%20from%20Here.md) | **Review** | Async in depth (tokio, JoinSet, async fn in traits, async closures, when to use), unsafe Rust (5 capabilities, 2024 safety improvements: unsafe_op_in_unsafe_fn, unsafe extern, static mut, newly unsafe fns), macros (declarative macro_rules!, procedural derive/attribute/function-like, syn/quote/proc-macro2), ecosystem map (web: axum/actix-web/reqwest/hyper/tonic/tower, CLI: clap/ratatui, serde, databases: sqlx/diesel/sea-orm, tracing/log, embedded: embedded-hal/embassy/esp-hal, WebAssembly: wasm-bindgen/leptos/dioxus), recommended reading (official docs, community books, interactive learning, staying current), concrete next-steps paths (web services, CLI tools, **embedded**, systems programming); **B-head capitalization fixed** (15 headings to title case → 16 with "With" → "with" fix), **crate name prose styling fixed** (embedded-hal/wasm-bindgen/anyhow/mio), **ecosystem verification date added**, **embedded path added to Your Next Steps**; **anti-stacking fixes** (12 lead-in sentences for Going Deeper lists, ecosystem tables, recommended reading subsections); `async_trait` verified still standard for dyn dispatch (March 2026); **backward reference fix** (replaced "Chapter 6" numbered reference with descriptive "the concurrency chapter"); `map!` macro output verified Rust 1.93.1 |

## Cross-Chapter Fixes

| Fix | Chapters Affected | Details |
|-----|-------------------|---------|
| **Numbered reference elimination** | 3.2, 4.1, 4.2, 5.1, 5.2, 6.1, 7.1, 7.3 | Replaced 18 numbered cross-references ("Chapter 1", "Part 2", "Part 3", etc.) with descriptive references ("the variables chapter", "the iterators chapter", "the concurrency chapter", etc.) for resilience to reordering and natural reading flow |
