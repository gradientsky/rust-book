# Chapter Tracker

Status legend: **Draft** | **Review** | **Final** | Planned

## Part 1: First Contact

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 1.1 Your First Rust Program | [`src/part1/1.1 Your First Rust Program.md`](../src/part1/1.1%20Your%20First%20Rust%20Program.md) | **Review** | Philosophy-first intro (beginner-accessible opening, no jargon), install, Cargo, printing/formatting, **format specifiers** (precision `{:.2}`, width/alignment `{:>10}`/`{:<10}`/`{:^10}`, zero-padding `{:05}`, hex `{:#x}`, binary `{:#b}`, combined specifiers, `format!` macro for building strings); first-mention styling for rustup/Cargo/crate; all 7 examples verified Rust 1.93.1 |
| 1.2 The Compiler Is Your Ally | [`src/part1/1.2 The Compiler Is Your Ally.md`](../src/part1/1.2%20The%20Compiler%20Is%20Your%20Ally.md) | **Review** | Error anatomy (E0382 with clone suggestion, E0308 with function-defined-here, unused variable warning), `cargo check`, `cargo clippy` (needless_range_loop with array, bool_comparison), **`dbg!` macro** (runtime value tracing, expression+value+location, stderr separation, pass-through return, `println!` vs `dbg!` guidance), `cargo fmt` with before/after, quality baseline workflow (updated to include `dbg!` step); all outputs verified Rust 1.93.1 |

## Part 2: Thinking in Values

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 2.1 Variables, Expressions, and Control Flow | [`src/part2/2.1 Variables, Expressions, and Control Flow.md`](../src/part2/2.1%20Variables,%20Expressions,%20and%20Control%20Flow.md) | **Review** | Immutability, shadowing, scalar types, type inference, **integer overflow** (debug panic/release wrap, checked/saturating/wrapping/strict methods), **`strict_*` dedicated section** (Rust 1.91: "Overflow that is always a bug" — debug-vs-release gap philosophy, `strict_add` financial example, drop-in `+` replacement with unconditional panic), **`is_multiple_of`** (Rust 1.87: intent-revealing divisibility check, leap year example, zero-safe), **`midpoint`** (Rust 1.85/1.87: overflow-safe averaging, binary search bug motivation, rounds toward zero, all integer+float types), **`cast_signed`/`cast_unsigned`** (Rust 1.87: explicit same-width sign conversion, prevents accidental truncation vs `as`, sensor byte example), constants, statics (`static` vs `const`, when to use each, **`AtomicU32` mutable global example**), **`LazyLock` lazy initialization** (runtime-computed statics, computed-once semantics, replaces `lazy_static`/`once_cell`), expressions, if/loop/while/for, ranges, loop labels, tuples/arrays, **`Vec<T>` mini-intro with `vec!`**, **`usize` array indexing demo**, if let, let chains (2024); all forward references replaced with inline explanations; **anti-stacking fix** (Control Flow A-head intro text), **forward reference fixes** (Vec "later chapters" → natural phrasing, `if let` Part 3 reference → self-contained description) |
| 2.2 Functions and Closures | [`src/part2/2.2 Functions and Closures.md`](../src/part2/2.2%20Functions%20and%20Closures.md) | **Review** | Functions (params, return, implicit return, early return), **placeholder macros (`todo!()`/`unimplemented!()`/`unreachable!()` — top-down development, intent communication, shipping_cost partial implementation example)**, expression-oriented bodies, closures (syntax, capture modes, Fn/FnMut/FnOnce traits), higher-order functions, returning closures, function factories; **fixed `apply_to_each`** (replaced hardcoded `[i32; 3]` return with proper slice iteration, consistent with `convert_temps` pattern); **fixed `move` forward reference** (replaced "you will see why in the next chapter" with self-contained explanation of ownership transfer, dangling borrow prevention, and natural bridge to `make_greeter`); all outputs verified Rust 1.94 |
| 2.3 Ownership | [`src/part2/2.3 Ownership.md`](../src/part2/2.3%20Ownership.md) | **Review** | Three rules of ownership, stack vs heap, move semantics, **moves into closures (`move` keyword callback from 2.2, closure outliving scope)**, Copy types, Clone, Drop/RAII, **drop on reassignment** (mutable variable reassignment drops previous value immediately, Noisy struct timing demo, ownership rule two connection), drop order, early drop, std::mem::drop; **added String::from vs string literal explanation** (D-head, `&str` vs `String` distinction, why String demonstrates ownership), **updated E0382 error output** to Rust 1.93.1 (column 5:22→5:21, added `help: consider cloning` suggestion with inline note), **refined data races bullet** (precise ownership+borrowing attribution); all 15 examples verified zero-warning Rust 1.93.1 |
| 2.4 Borrowing and References | [`src/part2/2.4 Borrowing and References.md`](../src/part2/2.4%20Borrowing%20and%20References.md) | **Review** | Shared refs (`&T`), mutable refs (`&mut T`), many-readers-or-one-writer rule, NLL, borrow checker errors (E0596/E0499/E0502/E0106), slices (`&str`/`&[T]`), dangling references, lifetimes (`'a`), **lifetime violation example (E0597 does_not_compile)**, lifetime elision, practical patterns; **updated E0596 error output** (added `help: consider changing this to be a mutable reference` suggestion), **updated E0106 error output** (added `help: return an owned value` suggestion), **heading case fix** (`Working with` lowercase preposition); all outputs verified Rust 1.93.1 |

## Part 3: Modeling Your Domain

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 3.1 Structs and Methods | [`src/part3/3.1 Structs and Methods.md`](../src/part3/3.1%20Structs%20and%20Methods.md) | **Review** | Struct definition, field init shorthand, mutation, struct update syntax (move visible in output), tuple structs, unit structs, methods (`&self`/`&mut self`/`self`), associated functions, `new` convention, `#[derive(Debug)]`, `Display` impl, ownership with structs, capstone example; **fixed dead_code/unused warnings** (all 18 examples zero-warning), **updated E0277 error output** to Rust 1.93.1; all outputs verified Rust 1.93.1 |
| 3.2 Enums and Pattern Matching | [`src/part3/3.2 Enums and Pattern Matching.md`](../src/part3/3.2%20Enums%20and%20Pattern%20Matching.md) | **Review** | ADTs (data-carrying variants, named fields), exhaustive `match` (E0004 with `note: defined here` + `help: todo!()` suggestion), match as expression, wildcard `_`, or patterns (`\|`), **`matches!` macro** (dedicated subsection, idiomatic boolean pattern check), `if let`, **"When to Prefer Match Over If Let"** (exhaustiveness note), **`let`-`else`** (guard clauses, happy path flat, divergent else block, when-to-use-each guide), let chains (Rust 2024), destructuring (enums, structs, tuples, nested), match guards, `..` ignore pattern, `Box<Expr>` recursive types, capstone Ticket example; **fixed dead_code/unused warnings** (all 20 examples zero-warning), **updated E0004 error output** to Rust 1.93.1; all outputs verified Rust 1.93.1 |
| 3.3 Null, Errors, and the Type System | [`src/part3/3.3 Null, Errors, and the Type System.md`](../src/part3/3.3%20Null,%20Errors,%20and%20the%20Type%20System.md) | **Review** | `Option<T>` (Some/None, unwrap_or, map, and_then, **`flatten` (nested Option collapse, HashMap<K, Option<V>> pattern, and_then relationship)**, is_some_and, is_none_or), `Result<T, E>` (Ok/Err, map, map_err, unwrap_or, **`flatten` (Rust 1.89: nested Result collapse, map-with-fallible-function pattern, and_then relationship)**), `?` operator (Result and Option, **automatic error conversion** — `&str`→`String` example demonstrating `From`-based conversion without forward reference), Option↔Result conversion (ok_or, ok), unwrap/expect guidance, making illegal states unrepresentable (enum vs boolean flags, newtype wrappers, validated construction, **module privacy for invariant enforcement**), capstone Score/grade example; **fixed dead_code warnings** (Connection enum uses both variants, Percentage destructures OutOfRange, fragile struct marked `rust,ignore`), **updated E0369 error output** to Rust 1.93.1; **fixed `?` operator forward reference** (replaced "From trait, which you will learn about in Part 4" with self-contained explanation and `validate`/`process` code example showing automatic `&str`→`String` conversion); all 22 examples verified Rust 1.93.1 |

## Part 4: Abstraction Without Cost

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 4.1 Traits | [`src/part4/4.1 Traits.md`](../src/part4/4.1%20Traits.md) | **Review** | Trait definition/impl, default methods, `impl Trait` params, derive (`Debug`/`Clone`/`PartialEq`/`Eq`/`Hash`/`Default`/`PartialOrd`/`Ord`/`Copy`), `Display` vs `Debug`, **`fmt::from_fn` (Rust 1.93: ad-hoc Display/Debug from closure, lazy evaluation, reusable `join` formatter returning `impl Display`)**, `From`/`Into`, `TryFrom`/`TryInto` (with `Display` error impl), `AsRef`, **supertraits (`trait Foo: Bar + Baz`, prerequisite traits, Loggable:Display example, links to Error:Debug+Display/Ord:Eq+PartialOrd/Copy:Clone)**, operator traits (`Add`), orphan rule, capstone temperature conversion example; **fixed dead_code/unused warnings** (all 17 examples zero-warning), **C-head case fix** (`Function Parameters` → `function parameters`), **heading hierarchy fix** (`from_fn` C-head promoted to B-head — was level-skipping under A-head without parent B-head), **forward reference fixes** (replaced "You will explore the full generics syntax in the next chapter" with self-contained monomorphization explanation, replaced "You will learn more about associated types in the next chapter" with self-contained associated type explanation, added inline generic syntax explanation for `join` function's `<'a, T: fmt::Display>`); all outputs verified Rust 1.93.1 |
| 4.2 Generics | [`src/part4/4.2 Generics.md`](../src/part4/4.2%20Generics.md) | **Review** | Generic functions/structs/enums, trait bounds (`T: Trait`), `impl Trait` (argument + return position, **same-type vs different-type code example**: `show_any`/`show_pair`), `where` clauses, **`impl Trait` in trait methods (RPITIT)** (Rust 1.75: `-> impl Trait` in trait method signatures, Summarizable example with Article/Tweet returning different iterator types, dyn-compatibility note), associated types vs type params, **associated type bounds (`Item: Display`, `Item: Display + Debug`, constraining vs pinning distinction, Rust 1.79/1.92)**, trait objects (`dyn Trait`), dyn compatibility (**added `impl Trait` return rule**), **trait upcasting (Rust 1.86: `dyn SubTrait` → `dyn SuperTrait` coercion, reference and Box examples, vtable pointer rewrite, one-way narrowing semantics)**, monomorphization/zero-cost abstraction, capstone Measurable example; **fixed `clone_self` dead_code warning** (added `#[derive(Clone)]` + concrete-type call), **updated E0369 error output** to Rust 1.93.1 (column 2:11→2:10); all 20 examples zero-warning, all outputs verified Rust 1.93.1 |
| 4.3 Iterators and Functional Patterns | [`src/part4/4.3 Iterators and Functional Patterns.md`](../src/part4/4.3%20Iterators%20and%20Functional%20Patterns.md) | **Review** | Iterator trait (`next`), three iteration modes (iter/iter_mut/into_iter), lazy evaluation, adaptors (map/filter/**`copied`/`cloned` (reference-to-owned conversion, `Copy` vs `Clone` cost distinction, `&&` solution)**/filter_map/enumerate/zip/take/skip/flatten/flat_map/chain/inspect), **`&&` double-reference pattern explanation** (why filter/find add `&Self::Item`, contrasted with map/any/all/position), **`array_windows::<N>()`** (Rust 1.94: compile-time-sized overlapping windows yielding `&[T; N]`, direct destructuring with `&[a, b]` pattern, day-over-day changes + moving average examples, `windows()` runtime-sized alternative noted), consumers (collect/fold/sum/product/count/any/all/find/position/min/max), **`total_cmp` for float sorting** (PartialOrd vs Ord, NaN safety, production-grade pattern), collecting into Vec/String/HashMap/HashSet/Result, ranges as iterators, `repeat_n`/`once`/`from_fn`/`successors`, implementing Iterator for custom types, IntoIterator, `is_sorted_by_key` (1.82), capstone GradeBook example; all outputs verified Rust 1.93.1 |

## Part 5: Building Real Things

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 5.1 Error Handling in Practice | [`src/part5/5.1 Error Handling in Practice.md`](../src/part5/5.1%20Error%20Handling%20in%20Practice.md) | **Review** | `Error` trait (Debug+Display, `source()`), error chains, thiserror 2.x (`#[error]`/`#[from]`/`#[source]`/`#[error(transparent)]`), anyhow 1.x (`Result`, `Context`/`with_context`, `bail!`/`ensure!`/`anyhow!`), `?` and `From::from()` composition, **`inspect_err`/`inspect` (Rust 1.76: observe errors in `?` chains without modifying, logging/debugging pattern, `Option::inspect` counterpart)**, library vs application error strategy, **fallible main (`fn main() -> Result<(), E>`, Termination trait, error type comparison table, `ExitCode` for specific exit codes, `run()` + `main()` pattern)**, unwrap spectrum, capstone Inventory example; **fixed AppError variant shapes** (distinct error types for `#[source]` vs `#[from]`), **added `Send + Sync` inline explanation**, **heading style fix** (lowercase crate names); all 15 examples zero-warning, all outputs verified Rust 1.93.1 |
| 5.2 Collections, Strings, and Smart Pointers | [`src/part5/5.2 Collections, Strings, and Smart Pointers.md`](../src/part5/5.2%20Collections,%20Strings,%20and%20Smart%20Pointers.md) | **Review** | Vec (creation/access/modify/slices/iteration), HashMap (creation/get/entry API), HashSet (membership/set ops), **BTreeMap/BTreeSet (sorted collections: sorted iteration, entry API, range queries with `.range()`, BTreeSet, Hash-vs-BTree decision table)**, String vs &str (ownership/conversion/UTF-8), Box (recursive types/trait objects), Rc (shared ownership/refcount), Arc (thread-safe shared ownership), **Cow (clone on write: borrow-vs-own deferral, `normalize` example, `String::from_utf8_lossy` std library usage, pointer decision table)**, capstone Library/Document tag system; **fixed `&&str` double-reference** in HashMap collect example (now uses `into_iter()` on array); **added `get_disjoint_mut`** (Rust 1.86: safe simultaneous mutable access to multiple Vec elements by index, philosophy-first explanation of borrow checker limitation); **added `extract_if`** (Rust 1.87/1.88: conditional removal + collection for Vec/HashMap/HashSet, philosophy-first explanation of retain limitation, lazy iterator semantics); **added `floor_char_boundary`/`ceil_char_boundary`** (Rust 1.91: safe string truncation at byte budgets, café ☕ example); **added `pop_if`** (Rust 1.86: conditional pop with closure predicate, philosophy-first peek-then-pop limitation, before/after comparison); **added `as_array::<N>()`** (Rust 1.93: safe slice-to-fixed-size-array conversion, RGB pixel parsing example, Option-based length check); **added `as_chunks::<N>()`** (Rust 1.88: compile-time-checked fixed-size slice splitting, 3D coordinates example with remainder handling, `chunks()` vs `as_chunks()` type-level distinction); **added slice patterns** (match on slice shape: `[]`/`[x]`/`[first, .., last]`/`rest @ ..`, Vec matching via `.as_slice()`, let-else guard clause, `@` subslice binding); all 36 examples zero-warning, all outputs verified Rust 1.94 |
| 5.3 Modules and Project Structure | [`src/part5/5.3 Modules and Project Structure.md`](../src/part5/5.3%20Modules%20and%20Project%20Structure.md) | **Review** | mod/use/pub, visibility spectrum (pub/pub(crate)/pub(super)/private), struct field visibility, re-exports (pub use), file layout (named-file convention), library vs binary crates, lib+bin pattern, multiple binaries (src/bin/), Cargo.toml (package metadata, dependencies, [lints], resolver v3), **`#[expect]` vs `#[allow]` (Rust 1.81: checked lint suppression, unfulfilled expectation warning, production-grade alternative to `#[allow]`)**, **automatic cache garbage collection (Rust 1.88: timestamp tracking from 1.78, daily cleanup of unused .crate archives and extracted sources, `[cache] auto-clean-frequency` config, CI caching caveat)**, workspaces (members, workspace.dependencies, workspace.lints), crate evaluation guide, capstone KeyValueStore example; **fixed dead_code/unused warnings** (added `describe` fn using private `is_even`, used `email` pub(crate) field, used FmtResult/IoResult aliases), **fixed anti-stacking** (Package Metadata/Dependencies headings), **renamed `default_settings` to `new`**; all outputs verified zero-warning Rust 1.93.1 |
| 5.4 Testing as a First-Class Citizen | [`src/part5/5.4 Testing as a First-Class Citizen.md`](../src/part5/5.4%20Testing%20as%20a%20First-Class%20Citizen.md) | **Review** | `#[test]`/`#[cfg(test)]`, assert family (assert!/assert_eq!/assert_ne! with custom messages), `#[should_panic(expected)]`, Result-returning tests (`?` in tests, `Box<dyn Error>`), `#[ignore]` with reason strings (shown inline in output), integration tests (_tests/_ directory, shared helpers), doc tests (` ``` `/`no_run`/`compile_fail`/`should_panic`, hidden `#` lines), combined doctests (Rust 2024), `cargo test` workflows (filtering, --show-output, --test-threads, --ignored, --include-ignored, --doc, --lib, --test, --no-fail-fast), capstone Calculator example (`#[derive(Default)]`); **fixed factorial `should_panic` doctest** (explicit bounds check instead of debug-mode overflow), **updated all test output** to Rust 1.93.1 format; all outputs verified Rust 1.93.1 |

## Part 6: Fearless Systems Programming

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 6.1 Concurrency Without Data Races | [`src/part6/6.1 Concurrency Without Data Races.md`](../src/part6/6.1%20Concurrency%20Without%20Data%20Races.md) | **Review** | Data race philosophy, `thread::spawn` (JoinHandle, move, `'static`), `thread::scope` (scoped threads, auto-join, **mutable borrowing with `split_at_mut` for parallel disjoint mutation**), Send/Sync (auto traits, Rc non-Send, Cell non-Sync), `Arc<T>` (atomic refcount), `Mutex<T>` (MutexGuard RAII, poisoning, lock scope), **`RwLock<T>` (many-readers-or-one-writer, read/write guards, **`RwLockWriteGuard::downgrade` (Rust 1.92: atomic write-to-read lock conversion, cache population pattern)**, Mutex vs RwLock guidance)**, `Arc<Mutex<T>>` pattern, channels (`mpsc::channel`, Sender/Receiver, multiple producers), **`io::pipe` (Rust 1.87: anonymous pipes for byte-level inter-thread I/O, `PipeReader`/`PipeWriter` with `Read`/`Write` traits, EOF on writer drop, process I/O redirection)**, **`File::lock` (Rust 1.89: cross-platform file locking, `try_lock`/`lock_shared`/`try_lock_shared` API, `TryLockError::WouldBlock`, advisory on Unix/enforced on Windows, automatic release on drop, lock file pattern)**, async taste (`async`/`await`, Future trait, tokio, `#[tokio::main]`), **async closures (Rust 1.85: `async \|\| {}`, `AsyncFn`/`AsyncFnMut`/`AsyncFnOnce` traits table, `AsyncFnMut` retry example, old `Fn() -> Fut` limitation for higher-ranked lifetimes)**, capstone worker pool; **heading style fix** (`The AsyncFn traits` → `The async closure traits`); all outputs verified Rust 1.93.1 |

## Part 7: Idiomatic Rust

| Chapter | File | Status | Notes |
|---------|------|--------|-------|
| 7.1 Patterns the Pros Use | [`src/part7/7.1 Patterns the Pros Use.md`](../src/part7/7.1%20Patterns%20the%20Pros%20Use.md) | **Review** | Builder (consuming, fallible build, **`#[must_use]` for catching forgotten `.build()` calls**), newtype (type safety, orphan rule workaround), type-state (PhantomData, compile-time state machines, **reject→revise→approve path**), combinators (map/and_then/filter/unwrap_or pipeline, **`Result` combinator quick reference table** with map_err/and_then/unwrap_or_else/ok/**`flatten`**, `parse_port` chain example), extension traits (blanket impls, FooExt convention), capstone document processing system; **fixed dead_code warnings** (removed `#[derive(Debug)]` from DatabaseConfig, accessed Email/Server fields directly, demonstrated reject path in type-state), **updated `#[must_use]` warning output** to Rust 1.93.1 (added `#[warn(unused_must_use)]` note, `help: use let _ =` suggestion); all 15 compilable examples zero-warning, all outputs verified Rust 1.93.1 |
| 7.2 What Not to Do | [`src/part7/7.2 What Not to Do.md`](../src/part7/7.2%20What%20Not%20to%20Do.md) | **Review** | Cloning to silence borrow checker (narrowing scopes, borrowed types), unwrap in production (Result/? propagation, unwrap spectrum), Java-in-Rust (getter/setter boilerplate, overusing dyn Trait, Deref inheritance, composition), fighting the type system (stringly-typed code, boolean blindness, invalid states), reaching for unsafe (split_at_mut, 2024 unsafe_op_in_unsafe_fn), overusing Rc/Arc (move/borrow first, decision table), index loops vs iterators (translations table), capstone Score/Grade report (uses `total_cmp` for float sorting); **B-head capitalization fixed** (17 headings to title case), **A-head "over" lowercase fix**, **anti-stacking fix** (lead-in sentence for Return Result section); all 25 examples zero-warning, all outputs verified Rust 1.93.1 |
| 7.3 Where to Go from Here | [`src/part7/7.3 Where to Go from Here.md`](../src/part7/7.3%20Where%20to%20Go%20from%20Here.md) | **Review** | Async in depth (tokio, JoinSet, async fn in traits, async closures, when to use), unsafe Rust (5 capabilities, 2024 safety improvements: unsafe_op_in_unsafe_fn, unsafe extern, static mut, newly unsafe fns), macros (declarative macro_rules!, procedural derive/attribute/function-like, syn/quote/proc-macro2), ecosystem map (web: axum/actix-web/reqwest/hyper/tonic/tower, CLI: clap/ratatui, serde, databases: sqlx/diesel/sea-orm, tracing/log, embedded: embedded-hal/embassy/esp-hal, WebAssembly: wasm-bindgen/leptos/dioxus), recommended reading (official docs, community books, interactive learning, staying current), concrete next-steps paths (web services, CLI tools, **embedded**, systems programming); **B-head capitalization fixed** (15 headings to title case), **crate name prose styling fixed** (embedded-hal/wasm-bindgen/anyhow/mio), **ecosystem verification date added**, **embedded path added to Your Next Steps**; `async_trait` verified still standard for dyn dispatch (March 2026); `map!` macro output verified Rust 1.93.1 |
